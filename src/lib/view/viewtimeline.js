export const timeLine = () => {
  const divTimeline = document.createElement('div');
  const viewTimeline = `
  <p>bienvenido a  mi muro</p>`;
  divTimeline.innerHTML = viewTimeline;
  return divTimeline;
};
